# Multi-host connection string; the client pool must survive a single node loss.
DB_URI="postgresql://root@crdb-linode-1,crdb-linode-2,crdb-azure-1,crdb-azure-2,crdb-gcp-1:26257/ycsb?sslmode=disable"
CRDBLAB_RUNS_DIR="runs"
