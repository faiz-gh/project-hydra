[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "crdblab"
version = "0.1.0"
description = "Measurement harness for a multi-cloud CockroachDB testbed"
requires-python = ">=3.10"
dependencies = [
    "pandas>=2.0",
    "numpy>=1.24",
    "matplotlib>=3.7",
    "seaborn>=0.13",
    "psycopg[binary]>=3.1",
    "PyYAML>=6.0",
    "python-dotenv>=1.0",
]

[project.optional-dependencies]
# python-docx is here rather than in the runtime dependencies on purpose:
# tools/make_docx.py renders the dissertation and touches no measurement, and
# every runtime pin is one more version a reproduction has to match.
dev = ["pytest>=8.0", "pytest-cov>=5.0", "ruff>=0.5", "python-docx>=1.1"]

[project.scripts]
crdblab = "crdblab.cli:main"

[tool.setuptools.packages.find]
include = ["crdblab*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
